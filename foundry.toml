[profile.default]
src = "src"
out = "out"
libs = ["lib"]
eth_rpc_url = "${ETH_RPC_URL}"

[fuzz]
runs = 1000

[invariant]
runs = 256
depth = 15
fail_on_revert = false

[rpc_endpoints]
mainnet = "${ETH_RPC_URL}"
